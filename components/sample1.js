//数値は　7　のようにそのまま記述する
// テキストの値は "こんにちは"　のように""で囲む
// 真偽値はfalseとtrueの二つのみ
// 変数宣言はvarとletの二つ
// varはscript全般
// letは変数を用意したところだけで使う
// var 変数名　=　値
// const 定数　= 値
// 数値計算は+-*/
// %にて余りを求められる
// 比較演算子は==や!=
// テキストは+を使って他のテキストやそれ以外の値と繋げられる
// 文の終わりは改行するか;をつけるかで示すが、両方使うのが一般的

let a = 2
let b = 3
let c = 10
if (a > 0) {
    a+b
} else {
    a-b
}

// switc構文についてjlさjf：dkぁsjfヵdjsflk：あsdjf；ぁsdkぁ

while (c > 0) {
    c = c - b
}

// forについてjsあ、fkぁsdjfkぁsjfさkfsd
// for (初期化 ;  条件; 後処理) {} //繰り返し
// for (変数 in 配列)

// 配列の書き方{a, b, c}
var line1 = new Array()
var line2 = new Array(要素の数)
var line3 = [値]
// 配列の変数名　[番号] = 値
// 変数　= 配列[番号]

// function 関数 (引数) {} //関数を作る時のキーワード

// var 変数 = function (引数) {}
// function 関数 (引数) {   return 値}

